const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { uniqueCode: 'TK-2EDC6A' }, include: { ownedProperties: true }}); console.log(user.ownedProperties); } main().finally(() => prisma.$disconnect());
